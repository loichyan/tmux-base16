# This file is @generated by tmux-base16
%if "#{!=:#{@base16-background},light}"
	# Scheme: Gruvbox Carbon Dark
	# Author: Sainnhe Park (https://github.com/sainnhe), Loi Chyan (https://github.com/loichyan)
	set -g @base00 "#0e0e0e"
	set -g @base01 "#1f1f1f"
	set -g @base02 "#35312f"
	set -g @base03 "#4b4440"
	set -g @base04 "#645a52"
	set -g @base05 "#ccb690"
	set -g @base06 "#d7c19c"
	set -g @base07 "#e3d2ab"
	set -g @base08 "#ea6962"
	set -g @base09 "#e78a4e"
	set -g @base0A "#d8a657"
	set -g @base0B "#a9b665"
	set -g @base0C "#89b482"
	set -g @base0D "#7daea3"
	set -g @base0E "#d3869b"
	set -g @base0F "#a89984"

	# Bright colors with lightness +5%
	set -g @base00-bright "#101010"
	set -g @base01-bright "#222222"
	set -g @base02-bright "#393533"
	set -g @base03-bright "#504945"
	set -g @base04-bright "#6b6159"
	set -g @base05-bright "#d9c39c"
	set -g @base06-bright "#e4cea9"
	set -g @base07-bright "#f1e0b9"
	set -g @base08-bright "#f6746c"
	set -g @base09-bright "#f3955a"
	set -g @base0A-bright "#e5b263"
	set -g @base0B-bright "#b5c271"
	set -g @base0C-bright "#94c08d"
	set -g @base0D-bright "#88b9ae"
	set -g @base0E-bright "#df91a6"
	set -g @base0F-bright "#b3a48e"

	# Dim colors with lightness -5%
	set -g @base00-dim "#0c0c0c"
	set -g @base01-dim "#1c1c1c"
	set -g @base02-dim "#312d2b"
	set -g @base03-dim "#463f3b"
	set -g @base04-dim "#5d534c"
	set -g @base05-dim "#bfaa84"
	set -g @base06-dim "#cab48f"
	set -g @base07-dim "#d5c49d"
	set -g @base08-dim "#de5e58"
	set -g @base09-dim "#db7f42"
	set -g @base0A-dim "#cc9a4b"
	set -g @base0B-dim "#9daa59"
	set -g @base0C-dim "#7ea977"
	set -g @base0D-dim "#72a398"
	set -g @base0E-dim "#c77b90"
	set -g @base0F-dim "#9d8e7a"
%else
	# Scheme: Gruvbox Carbon Light
	# Author: Sainnhe Park (https://github.com/sainnhe), Loi Chyan (https://github.com/loichyan)
	set -g @base00 "#fff2ca"
	set -g @base01 "#f7e6be"
	set -g @base02 "#e2d1ae"
	set -g @base03 "#b2a38e"
	set -g @base04 "#998a7b"
	set -g @base05 "#433f3d"
	set -g @base06 "#2d2d2d"
	set -g @base07 "#1d1d1d"
	set -g @base08 "#9d0006"
	set -g @base09 "#af3a03"
	set -g @base0A "#a26700"
	set -g @base0B "#79740e"
	set -g @base0C "#427b58"
	set -g @base0D "#076678"
	set -g @base0E "#8f3f71"
	set -g @base0F "#7c6f64"

	# Bright colors with lightness -5%
	set -g @base00-bright "#efe2bb"
	set -g @base01-bright "#e8d7af"
	set -g @base02-bright "#d4c3a0"
	set -g @base03-bright "#a79883"
	set -g @base04-bright "#8f8072"
	set -g @base05-bright "#3e3a38"
	set -g @base06-bright "#292929"
	set -g @base07-bright "#1a1a1a"
	set -g @base08-bright "#950000"
	set -g @base09-bright "#a63100"
	set -g @base0A-bright "#995f00"
	set -g @base0B-bright "#716c00"
	set -g @base0C-bright "#3a7351"
	set -g @base0D-bright "#005f71"
	set -g @base0E-bright "#87386a"
	set -g @base0F-bright "#74675c"

	# Dim colors with lightness +5%
	set -g @base00-dim "#ffffda"
	set -g @base01-dim "#fff5cd"
	set -g @base02-dim "#f0dfbc"
	set -g @base03-dim "#bdae99"
	set -g @base04-dim "#a39485"
	set -g @base05-dim "#484442"
	set -g @base06-dim "#313131"
	set -g @base07-dim "#202020"
	set -g @base08-dim "#a51110"
	set -g @base09-dim "#b84212"
	set -g @base0A-dim "#ab6f14"
	set -g @base0B-dim "#817c1c"
	set -g @base0C-dim "#4a8360"
	set -g @base0D-dim "#156d7f"
	set -g @base0E-dim "#974678"
	set -g @base0F-dim "#84776c"
%endif
