# This file is @generated by tmux-base16
%if "#{!=:#{@base16-background},light}"
	%usepalette dark
	# Scheme: ${scheme}
	# Author: ${author}
	set -g @base00 "#${base00}"
	set -g @base01 "#${base01}"
	set -g @base02 "#${base02}"
	set -g @base03 "#${base03}"
	set -g @base04 "#${base04}"
	set -g @base05 "#${base05}"
	set -g @base06 "#${base06}"
	set -g @base07 "#${base07}"
	set -g @base08 "#${base08}"
	set -g @base09 "#${base09}"
	set -g @base0A "#${base0A}"
	set -g @base0B "#${base0B}"
	set -g @base0C "#${base0C}"
	set -g @base0D "#${base0D}"
	set -g @base0E "#${base0E}"
	set -g @base0F "#${base0F}"
	%endpalette dark
	%usepalette dark-bright

	# Bright colors with lightness ${bright}
	set -g @base00-bright "#${base00}"
	set -g @base01-bright "#${base01}"
	set -g @base02-bright "#${base02}"
	set -g @base03-bright "#${base03}"
	set -g @base04-bright "#${base04}"
	set -g @base05-bright "#${base05}"
	set -g @base06-bright "#${base06}"
	set -g @base07-bright "#${base07}"
	set -g @base08-bright "#${base08}"
	set -g @base09-bright "#${base09}"
	set -g @base0A-bright "#${base0A}"
	set -g @base0B-bright "#${base0B}"
	set -g @base0C-bright "#${base0C}"
	set -g @base0D-bright "#${base0D}"
	set -g @base0E-bright "#${base0E}"
	set -g @base0F-bright "#${base0F}"
	%endpalette dark-bright
	%usepalette dark-dim

	# Dim colors with lightness ${dim}
	set -g @base00-dim "#${base00}"
	set -g @base01-dim "#${base01}"
	set -g @base02-dim "#${base02}"
	set -g @base03-dim "#${base03}"
	set -g @base04-dim "#${base04}"
	set -g @base05-dim "#${base05}"
	set -g @base06-dim "#${base06}"
	set -g @base07-dim "#${base07}"
	set -g @base08-dim "#${base08}"
	set -g @base09-dim "#${base09}"
	set -g @base0A-dim "#${base0A}"
	set -g @base0B-dim "#${base0B}"
	set -g @base0C-dim "#${base0C}"
	set -g @base0D-dim "#${base0D}"
	set -g @base0E-dim "#${base0E}"
	set -g @base0F-dim "#${base0F}"
	%endpalette dark-dim
%else
	%usepalette light
	# Scheme: ${scheme}
	# Author: ${author}
	set -g @base00 "#${base00}"
	set -g @base01 "#${base01}"
	set -g @base02 "#${base02}"
	set -g @base03 "#${base03}"
	set -g @base04 "#${base04}"
	set -g @base05 "#${base05}"
	set -g @base06 "#${base06}"
	set -g @base07 "#${base07}"
	set -g @base08 "#${base08}"
	set -g @base09 "#${base09}"
	set -g @base0A "#${base0A}"
	set -g @base0B "#${base0B}"
	set -g @base0C "#${base0C}"
	set -g @base0D "#${base0D}"
	set -g @base0E "#${base0E}"
	set -g @base0F "#${base0F}"
	%endpalette light
	%usepalette light-bright

	# Bright colors with lightness ${bright}
	set -g @base00-bright "#${base00}"
	set -g @base01-bright "#${base01}"
	set -g @base02-bright "#${base02}"
	set -g @base03-bright "#${base03}"
	set -g @base04-bright "#${base04}"
	set -g @base05-bright "#${base05}"
	set -g @base06-bright "#${base06}"
	set -g @base07-bright "#${base07}"
	set -g @base08-bright "#${base08}"
	set -g @base09-bright "#${base09}"
	set -g @base0A-bright "#${base0A}"
	set -g @base0B-bright "#${base0B}"
	set -g @base0C-bright "#${base0C}"
	set -g @base0D-bright "#${base0D}"
	set -g @base0E-bright "#${base0E}"
	set -g @base0F-bright "#${base0F}"
	%endpalette light-bright
	%usepalette light-dim

	# Dim colors with lightness ${dim}
	set -g @base00-dim "#${base00}"
	set -g @base01-dim "#${base01}"
	set -g @base02-dim "#${base02}"
	set -g @base03-dim "#${base03}"
	set -g @base04-dim "#${base04}"
	set -g @base05-dim "#${base05}"
	set -g @base06-dim "#${base06}"
	set -g @base07-dim "#${base07}"
	set -g @base08-dim "#${base08}"
	set -g @base09-dim "#${base09}"
	set -g @base0A-dim "#${base0A}"
	set -g @base0B-dim "#${base0B}"
	set -g @base0C-dim "#${base0C}"
	set -g @base0D-dim "#${base0D}"
	set -g @base0E-dim "#${base0E}"
	set -g @base0F-dim "#${base0F}"
	%endpalette light-dim
%endif
